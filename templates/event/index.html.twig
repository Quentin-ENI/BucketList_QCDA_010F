{% extends 'base.html.twig' %}



{% block main %}

    {{ form_start(eventForm) }}
    {{ form_widget(eventForm) }}
    <button type="submit">Create</button>
    {{ form_end(eventForm) }}

{% endblock %}
